export enum EProcessStatus {
  NOT_START = "not_start",
  STARTED = "started",
  STOPPED = "stopped",
}

export enum EProcessAction {
  START = "start",
  RESTART = "restart",
  DELETE = "delete",
  STOP = "stop",
}
